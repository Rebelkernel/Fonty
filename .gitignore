# ---- Node / Vite / frontend ----
node_modules
dist
dist-ssr
*.local
.vite
.vite-inspect

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ---- Rust / Tauri ----
# Cargo build artefacts
src-tauri/target
target

# Full-bundle output goes to GitHub Releases, not into the repo
src-tauri/target/release/bundle

# Rust scratch / debug artefacts
*.rs.bk
*.pdb

# ---- Editor / OS ----
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ---- FONTY scratch files ----
# Dev helpers that live in the repo root but don't need version control
icon-source.png
*.db
*.db-journal
*.db-wal
*.db-shm

# ---- Claude Code local state (machine-specific, not public) ----
.claude/

